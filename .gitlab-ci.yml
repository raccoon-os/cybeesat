variables:
  GIT_SUBMODULE_STRATEGY: recursive
  GIT_SUBMODULE_FORCE_HTTPS: "true"

stages:
  - build

build-job:
  stage: build
  script:
    - rm -rf install/usr/bin/*
    - make deploy
  artifacts:
    paths:
      - install